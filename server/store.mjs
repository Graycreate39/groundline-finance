import fs from 'node:fs';import path from 'node:path';
export class LocalStore{constructor(file=process.env.GROUNDLINE_DATA_PATH||'.groundline/data.json'){this.file=path.resolve(file);fs.mkdirSync(path.dirname(this.file),{recursive:true});if(!fs.existsSync(this.file))this.write({version:1,workspaces:[],audit:[]});}read(){return JSON.parse(fs.readFileSync(this.file,'utf8'));}write(data){const tmp=`${this.file}.tmp`;fs.writeFileSync(tmp,JSON.stringify(data,null,2),{mode:0o600});fs.renameSync(tmp,this.file);}saveResearch(result,objective){const db=this.read(),id=`ws-${result.identity.cik}`;const existing=db.workspaces.find(x=>x.id===id);const workspace={id,kind:'live-research',objective,createdAt:existing?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString(),...result};const i=db.workspaces.findIndex(x=>x.id===id);if(i<0)db.workspaces.push(workspace);else db.workspaces[i]=workspace;db.audit.push({id:`audit-${Date.now()}`,workspaceId:id,action:'research.completed',at:workspace.updatedAt,actor:'SEC adapter'});this.write(db);return workspace;}list(){return this.read().workspaces.map(({evidence,...x})=>({...x,claimCount:evidence?.claims?.length||0}));}}
