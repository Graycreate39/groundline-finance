import fs from 'node:fs';fs.rmSync('dist',{recursive:true,force:true});for(const dir of ['src','assets'])fs.mkdirSync(`dist/${dir}`,{recursive:true});for(const f of ['index.html','tokens.css','src/app.mjs','src/styles.css','src/engine.mjs'])fs.copyFileSync(f,`dist/${f}`);for(const dir of ['assets','screenshots','docs'])fs.cpSync(dir,`dist/${dir}`,{recursive:true});console.info('Built research-first interface to dist/');
