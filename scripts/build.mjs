import fs from 'node:fs';fs.rmSync('dist',{recursive:true,force:true});fs.mkdirSync('dist/src/data',{recursive:true});for(const f of ['index.html','src/app.mjs','src/styles.css','src/engine.mjs','src/data/demo.mjs'])fs.copyFileSync(f,`dist/${f}`);for(const dir of ['screenshots','docs'])fs.cpSync(dir,`dist/${dir}`,{recursive:true});console.info('Built local static application to dist/');
