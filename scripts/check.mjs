import fs from 'node:fs';const files=['index.html','src/app.mjs','src/styles.css','src/engine.mjs','src/data/demo.mjs'];for(const f of files){if(!fs.existsSync(f))throw new Error(`Missing ${f}`)};for(const f of ['src/app.mjs','src/engine.mjs']){const t=fs.readFileSync(f,'utf8');if(/console\.log|TODO/.test(t))throw new Error(`Quality marker in ${f}`)}console.info('Static quality checks passed');
