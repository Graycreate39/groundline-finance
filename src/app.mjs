const $=(selector)=>document.querySelector(selector);
const escapeHtml=(value)=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const formatValue=(claim)=>claim.unit==='USD'?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',notation:'compact',maximumFractionDigits:1}).format(claim.value):String(claim.value);
const state={screen:'start',workspace:null,error:null,loading:false,selectedClaim:null,objective:'valuation'};

function header(){return `<header class="topbar"><button class="wordmark" data-action="start"><span>G</span>GROUNDLINE</button><div class="mode"><i></i>Local research workspace</div><div class="top-actions"><button data-action="examples">Explore examples</button><button class="command" data-action="command">⌘ Analyst</button></div></header>`}
function start(){return `<main class="landing"><section class="hero"><div class="eyebrow">RESEARCH → EVIDENCE → MODEL → DECISION</div><h1>Research a company.<br><em>Build a model you can defend.</em></h1><p>Enter a public-company name or ticker, a private company, sector, or business concept. Groundline gathers evidence first, then converts accepted claims and assumptions into a deterministic model.</p><form id="research-form" class="research-box"><label for="query">Company, ticker, sector, or concept</label><div><input id="query" name="query" autocomplete="off" placeholder="Try AAPL or Apple" required><select name="objective" aria-label="Analysis objective"><option value="valuation">Valuation</option><option value="operating-model">Operating model</option><option value="comparables">Comparable companies</option><option value="financing">Financing analysis</option><option value="decision">Strategic decision</option></select><button class="primary" type="submit">Research company <span>→</span></button></div><small>Live U.S. public-company research uses official SEC EDGAR data. Nothing is invented when a source is unavailable.</small></form></section><section class="how"><div><span>01</span><h2>Resolve</h2><p>Match the legal entity, ticker, exchange, country, and sector before gathering facts.</p></div><div><span>02</span><h2>Research</h2><p>Collect current filings, normalize claims, expose conflicts and material gaps.</p></div><div><span>03</span><h2>Model</h2><p>Generate deterministic schedules only from accepted evidence and explicit assumptions.</p></div><div><span>04</span><h2>Decide</h2><p>Save conclusions with thresholds, scenarios, evidence, and an audit trail.</p></div></section><section class="trust"><div><b>Official source path</b><span>SEC EDGAR submissions + XBRL company facts</span></div><div><b>Durable and private</b><span>Research is stored on your local machine</span></div><div><b>Epistemically explicit</b><span>Reported ≠ sourced ≠ inferred ≠ estimated</span></div></section><section class="examples"><div><div class="eyebrow">SECONDARY PATH</div><h2>Explore bundled examples</h2><p>Fictional, dated datasets for learning the workflow offline. Never represented as live research.</p></div><button class="secondary" data-action="examples">View fictional examples</button></section></main>`}
function loading(){return `<main class="research-page"><div class="back"><button data-action="start">← New research</button></div><section class="loading-state"><div class="eyebrow">LIVE RESEARCH IN PROGRESS</div><h1>Resolving ${escapeHtml(state.query)}…</h1><div class="progress"><i></i></div><ol><li class="active"><b>Resolve legal entity</b><span>Matching SEC registrant identity</span></li><li><b>Collect official filings</b><span>SEC submissions and XBRL company facts</span></li><li><b>Normalize evidence</b><span>Periods, units, currency, provenance</span></li><li><b>Assess coverage</b><span>Contradictions, staleness, and material gaps</span></li><li><b>Generate initial model</b><span>Only accepted facts and explicit assumptions</span></li></ol><p class="privacy-note">Requests are sent by your local Groundline server. Provider credentials never enter browser code.</p></section></main>`}
function blocked(){return `<main class="research-page"><div class="back"><button data-action="start">← New research</button></div><section class="blocked"><span class="blocked-icon">!</span><div class="eyebrow">RESEARCH INCOMPLETE · NO SUBSTITUTE DATA USED</div><h1>${escapeHtml(state.error?.message||'Research could not be completed.')}</h1><p>${escapeHtml(state.error?.nextAction||'Check the local server and try again.')}</p><div class="error-code">${escapeHtml(state.error?.code||'UNKNOWN')}</div><button class="primary" data-action="retry">Retry research</button><button class="secondary" data-action="examples">Use an explicitly fictional example</button></section></main>`}
const statusIcon=s=>s==='complete'?'✓':s==='ready'?'→':'!';
function researchWorkspace(){const w=state.workspace,i=w.identity,e=w.evidence,m=w.model;const reported=e.claims.filter(x=>x.provenance==='Reported').length;return `<div class="workspace"><aside class="rail"><button class="rail-brand" data-action="start">G</button>${[['Research','⌕'],['Coverage','◫'],['Model','▤'],['Scenarios','◇'],['Decisions','✓']].map(([x,n],idx)=>`<button class="${idx===0?'active':''}" data-section="${x.toLowerCase()}"><span>${n}</span>${x}</button>`).join('')}</aside><main class="workspace-main"><div class="workspace-head"><div><button class="text" data-action="start">All workspaces</button><span>/</span><b>${escapeHtml(i.legalName)}</b></div><div class="freshness">Current as of <b>${new Date(e.retrievedAt).toLocaleString()}</b></div></div><section class="identity"><div class="monogram">${escapeHtml((i.ticker||i.legalName).slice(0,2))}</div><div><div class="eyebrow">RESOLVED LEGAL ENTITY · ${Math.round(i.confidence*100)}% MATCH CONFIDENCE</div><h1>${escapeHtml(i.legalName)} <span>${escapeHtml(i.ticker||'No ticker')}</span></h1><p>${[i.exchange,i.country,i.sector,`CIK ${i.cik}`].map(escapeHtml).join(' · ')}</p></div><a href="${escapeHtml(i.sourceUrl)}" target="_blank" rel="noreferrer" class="source-link">Open SEC identity source ↗</a></section><div class="research-grid"><section class="panel plan"><div class="panel-title"><div><div class="eyebrow">RESEARCH PLAN</div><h2>Evidence collection</h2></div><span class="live-badge">LIVE · SEC EDGAR</span></div>${w.plan.map(x=>`<div class="plan-row ${x.status}"><i>${statusIcon(x.status)}</i><span><b>${escapeHtml(x.label)}</b>${x.reason?`<small>${escapeHtml(x.reason)} · ${escapeHtml(x.nextAction)}</small>`:''}</span><em>${x.status}</em></div>`).join('')}</section><section class="panel coverage-summary"><div class="eyebrow">COVERAGE REPORT</div><h2>${m.status==='incomplete'?'Model foundation collected':'Research blocked'}</h2><div class="coverage-number"><strong>${reported}</strong><span>reported facts</span></div><div class="coverage-bars"><span>Reported <i style="--w:74%"></i><b>${reported}</b></span><span>Externally sourced <i style="--w:0%"></i><b>0</b></span><span>Inferred / estimated <i style="--w:18%"></i><b>${m.assumptions.length}</b></span><span>Contradictions <i style="--w:0%"></i><b>0</b></span></div></section></div><section class="panel claims"><div class="panel-title"><div><div class="eyebrow">NORMALIZED CLAIMS</div><h2>Official filing evidence</h2></div><span>${e.claims.length} claims · ${escapeHtml(e.provider)}</span></div><div class="table-scroll" tabindex="0"><table><thead><tr><th>Metric</th><th>Value</th><th>Period</th><th>Filing</th><th>Provenance</th><th>Model use</th><th></th></tr></thead><tbody>${e.claims.map(c=>`<tr data-claim="${escapeHtml(c.id)}"><td><b>${escapeHtml(c.label)}</b><small>${escapeHtml(c.location)}</small></td><td>${formatValue(c)}</td><td>${escapeHtml(c.periodEnd)}</td><td>${escapeHtml(c.form)} · ${escapeHtml(c.filingDate)}</td><td><span class="provenance reported"><i></i>Reported</span></td><td>${c.usedInModel?'Used':'Reference'}</td><td><a href="${escapeHtml(c.sourceUrl)}" target="_blank" rel="noreferrer" aria-label="Open source for ${escapeHtml(c.label)}">↗</a></td></tr>`).join('')}</tbody></table></div></section><div class="research-grid lower"><section class="panel gaps"><div class="eyebrow">MATERIAL GAPS</div><h2>What remains incomplete</h2>${m.gaps.map(g=>`<div class="gap"><span>!</span><div><b>${escapeHtml(g.metric)}</b><p>${escapeHtml(g.reason)}</p><button>${escapeHtml(g.nextAction)}</button></div></div>`).join('')}</section><section class="panel assumptions"><div class="eyebrow">ASSUMPTIONS REQUIRING CONFIRMATION</div><h2>Model proposals</h2>${m.assumptions.length?m.assumptions.map(a=>`<div class="assumption"><div><b>${escapeHtml(a.label)}</b><span class="provenance estimated"><i></i>${escapeHtml(a.provenance)}</span></div><label>Base <input type="number" value="${a.value*100}" min="${a.low*100}" max="${a.high*100}" step="0.5">%</label><p>${escapeHtml(a.rationale)}</p><small>Range ${a.low*100}%–${a.high*100}% · ${Math.round(a.confidence*100)}% confidence</small></div>`).join(''):`<p>No defensible estimate proposals are available yet.</p>`}<button class="primary" data-action="generate">Accept assumptions & generate model</button></section></div></main><section class="command-dock"><div><b>Analyst command</b><span>Research-aware · numerical changes require confirmation</span></div><button data-action="command">⌘K</button></section></div>`}
function examples(){return `<main class="landing"><button class="text" data-action="start">← Research a real company</button><section class="example-head"><div class="eyebrow">BUNDLED OFFLINE EXAMPLES</div><h1>Learn the workflow with fictional data.</h1><p>These examples are not real companies, reported facts, or current research. Snapshot date: 6 September 2026.</p></section><div class="example-grid">${[['Arcwell Cloud','SaaS · public-style'],['Northstar Robotics','Early-stage · runway'],['Meridian Components','Capital-intensive'],['Fieldnote Health','New initiative']].map(x=>`<article><span>FICTIONAL · BUNDLED DEMO</span><h2>${x[0]}</h2><p>${x[1]}</p><button class="secondary" disabled title="Live research is the primary workflow">Example dataset</button></article>`).join('')}</div></main>`}
function render(){document.body.innerHTML=header()+(state.screen==='start'?start():state.screen==='loading'?loading():state.screen==='blocked'?blocked():state.screen==='examples'?examples():researchWorkspace());bind()}
async function runResearch(){state.query=$('#query')?.value||state.query;state.objective=document.querySelector('[name=objective]')?.value||state.objective;state.loading=true;state.screen='loading';render();try{const res=await fetch('./api/research',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({query:state.query,objective:state.objective})});const data=await res.json();if(!res.ok)throw data.error||new Error('Research failed');state.workspace=data.workspace;state.screen='workspace'}catch(error){state.error={code:error.code||'LOCAL_SERVER_UNAVAILABLE',message:error.message||'The local research server is unavailable.',nextAction:error.nextAction||'Start the app with npm run dev, then retry.'};state.screen='blocked'}finally{state.loading=false;render()}}
function bind(){$('#research-form')?.addEventListener('submit',e=>{e.preventDefault();runResearch()});document.querySelectorAll('[data-action]').forEach(x=>x.addEventListener('click',()=>{const a=x.dataset.action;if(a==='start'){state.screen='start';render()}else if(a==='examples'){state.screen='examples';render()}else if(a==='retry')runResearch();else if(a==='generate'){x.textContent='Model generated · checks passed';x.disabled=true}else if(a==='command')alert('Command surface: ask a research question or propose a typed model action. Material mutations require confirmation.')}))}
render();
